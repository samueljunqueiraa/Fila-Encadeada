# Fila Encadeada em Java

Este repositório contém a implementação de uma **Fila Encadeada (Linked Queue)**, desenvolvida como parte da disciplina de **Estrutura de Dados** no final de 2024, durante o curso de Sistemas de Informação no IFSULDEMINAS.

## 📌 Sobre o Projeto

A fila é uma estrutura de dados linear que segue o princípio **FIFO (First-In, First-Out)**, onde o primeiro elemento a ser inserido é o primeiro a ser removido. 

Nesta implementação encadeada, cada elemento (nó) contém o dado e uma referência para o próximo nó, permitindo que a estrutura cresça dinamicamente sem a necessidade de definir um tamanho fixo inicial, ao contrário de filas baseadas em arrays.


## 🚀 Funcionalidades Implementadas

O projeto inclui as operações fundamentais de uma fila:

* **Enqueue:** Adiciona um novo elemento ao final da fila.
* **Dequeue:** Remove o elemento que está no início da fila.
* **Peek/Front:** Retorna o elemento do início sem removê-lo.
* **isEmpty:** Verifica se a fila está vazia.
* **Size:** Retorna a quantidade de elementos presentes.

## 🛠️ Tecnologias Utilizadas

* **Linguagem:** Java 
* **IDE:** NetBeans 
* **Conceitos:** Orientação a Objetos, Alocação Dinâmica de Memória, Referências/Ponteiros.

## 📁 Estrutura de Pastas

* `src/javafilaencadeadamodelo`: Contém as classes de lógica da estrutura (Nó e Fila).
* `nbproject`: Arquivos de configuração do projeto NetBeans.

## ⚙️ Como Executar

1. Certifique-se de ter o **JDK 11 ou superior** instalado.
2. Clone o repositório:
   
   ```bash
   git clone [https://github.com/samueljunqueiraa/Fila-Encadeada.git](https://github.com/samueljunqueiraa/Fila-Encadeada.git)
   ```
   
4. Abra o projeto em sua IDE de preferência (recomendado NetBeans ou VS Code com extensões Java).
5. Localize a classe principal (provavelmente dentro de src) e execute o arquivo.

### Desenvolvido por Samuel Junqueira 
